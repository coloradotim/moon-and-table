# Issue 288 Direct-Use Review: Whitehurst Flower Methods

Date: 2026-06-11

Scope: 12 general flower-method rituals imported from `Whitehurst flower magic`.

Outcome: all 12 reviewed for direct search/library use.

Direct-use status:

- `status`: `reviewed`
- `availability.findable`: `true`
- `availability.directUseEligible`: `true`
- `availability.recommendationEligible`: `false`
- recommendation eligibility still blocked on `recommendation_review`

Promoted rituals:

- `whitehurst-flower-on-the-table`
- `whitehurst-vase-as-witness`
- `whitehurst-bowl-of-petals`
- `whitehurst-bouquet-with-intention`
- `whitehurst-blossom-contemplation`
- `whitehurst-short-spoken-purpose`
- `whitehurst-journal-after-flower`
- `whitehurst-ask-before-gathering`
- `whitehurst-small-offering`
- `whitehurst-choose-by-affinity`
- `whitehurst-ask-the-flower-here`
- `whitehurst-receive-flower-quality`

Repairs made:

- Removed user-facing `reviewed flower` / `reviewed bouquet` product-meta language from practice copy where it appeared.
- Rewrote extraction-style `whyThisFits` fields into Moon & Table rationale.
- Kept functional source boundaries intact: no long source charms, recipes, flower baths, oils, essences, or health claims were imported.
- Kept direct-use eligibility separate from recommendation eligibility.

Headline/body fit:

- Converted packet-style lowercase headlines with periods into app-ready title case.
- `Set one flower on the table.` became `Set One Flower on the Table`.
- `Let the vase witness the room.` became `Let the Vase Witness the Room`.
- `Put petals in a bowl.` became `Put Petals in a Bowl`.
- `Send the intention into the bouquet.` became `Send the Intention Into the Bouquet`.
- `Sit with the blossom.` became `Sit With the Blossom`.
- `Speak the flower's purpose once.` became `Speak the Flower's Purpose Once`.
- `Record what the flower held.` became `Record What the Flower Held`.
- `Ask before gathering.` became `Ask Before Gathering`.
- `Return thanks to the flower.` became `Return Thanks to the Flower`.
- `Let the flower choose first.` became `Let the Flower Choose First`.
- `Ask the flower that is here.` became `Ask the Flower That Is Here`.
- `Receive the flower's quality.` became `Receive the Flower's Quality`.

House-voice notes:

- These work best when kept tiny: one flower, one vessel, one sentence, one question, one line in the record.
- The batch had strong Moon & Table bones, but the import wording made it feel like a source-card demo. The overlay makes the same rituals feel more like direct household grimoire entries.

Held back:

- None in this sub-batch.

