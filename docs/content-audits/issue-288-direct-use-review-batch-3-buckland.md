# Issue 288 Direct-Use Review: Buckland Candle Batch

Date: 2026-06-11

Scope: 13 draft rituals imported from `Buckland, Practical Candleburning Rituals`.

Outcome: all 13 reviewed for direct search/library use.

Direct-use status:

- `status`: `reviewed`
- `availability.findable`: `true`
- `availability.directUseEligible`: `true`
- `availability.recommendationEligible`: `false`
- recommendation eligibility still blocked on `recommendation_review`

Promoted rituals:

- `ritual-buckland-candle-prepare-table`
- `ritual-candlelight-buckland-steadying-blue-meditation`
- `ritual-candlelight-buckland-releasing-habit-surrounded`
- `ritual-candlelight-buckland-tending-home-settling`
- `ritual-candlelight-buckland-marking-seven-night-increase`
- `ritual-candlelight-buckland-protecting-boundary-circle`
- `ritual-candlelight-buckland-remembering-photo-peace-light`
- `ritual-candlelight-buckland-blessing-object-in-light`
- `ritual-buckland-candle-courage-circle`
- `ritual-buckland-candle-welcome-joy`
- `ritual-buckland-candle-purify-self`
- `ritual-buckland-candle-clear-words`
- `ritual-buckland-candle-dream-door`

Repairs made:

- Removed process-gate language from direct-use presentation fields, including `material review`, `direct-use presentation still requires`, and private approval language.
- Rewrote extraction-style `whyThisFits` copy into user-facing Moon & Table rationale while preserving the source candle architecture.
- Removed packet residue from promoted practice copy, including `Use the first version for this candidate` and `Use the second version for this candidate`.
- Removed the remaining packet residue `Use the first protection version` from `Set the Boundary Light`.
- Removed private approval residue from the user-facing remembrance ritual body.
- Changed `Dream Door Candle` to `Open the Dream Door` so the headline names the ritual action rather than reading like a generic candle label.
- Changed `Boundary Light` to `Set the Boundary Light`, `Object in the Light` to `Bless the Object in Light`, and `Purify the Self Candle` to `Purify the Self by Candle` during the headline/body back-pass.
- Changed two gendered Buckland spoken lines for direct use: `his spirit` became `its spirit`, and `he sees` became `it sees`. The `ritualWords` metadata now records those as direct-use neutrality adaptations.
- Kept direct-use eligibility separate from recommendation eligibility.

House-voice notes:

- The batch is formally heavier than the Woodward and House Witch batches because Buckland candle rites are structured, repeated, and material-specific. That heaviness is appropriate to the source and should remain discoverable for direct search.
- `Set the Boundary Light`, `Bless the Object in Light`, and `Open the Dream Door` still feel more ceremonial than domestic. I left that weight because it is the point of this source batch; the direct flow should be allowed to surface a more formal candle rite when searched deliberately.

Smells worked:

- Two exact short source lines had gendered wording: `his spirit` and `he sees`. At user request, both were changed to `it` forms for direct use and annotated in `ritualWords`.

Held back:

- None in this batch.
